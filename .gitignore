# We're only holding on to specific files in .vim
# Things like keep_view will store dirs (/view) in .vim
.vim/*
.vim/vim_temp_files/*

!.vim/plugins
!.vim/autoload
!.vim/syntax
!.vim/vim_temp_files

# Only save .vim/bundle, not the links inside it
.vim/bundle/*
!.vim/bundle/.gitkeep

# Ignore the .swp files
*.swp
